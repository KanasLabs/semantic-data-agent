title = "semantic-data-agent gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Generated discovery and schema integrity hashes"
regexTarget = "line"
regexes = [
  '''"(?:single_key_check_hash|composite_key_check_hash|key_profile|ddl)"\s*:\s*"[0-9a-f]{64}"''',
]
