title = "semantic-data-agent gitleaks configuration"

[extend]
useDefault = true

[[allowlists]]
description = "Generated discovery integrity hashes whose field names contain key"
regexTarget = "line"
regexes = [
  '''"(?:single_key_check_hash|composite_key_check_hash)"\s*:\s*"[0-9a-f]{64}"''',
]
